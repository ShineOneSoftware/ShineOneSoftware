---
import Button from "@components/Button/Button.astro";
import FooterLink from "@components/Footer/FooterLink.astro";
import SiteLogo from "@components/SiteLogo/SiteLogo.astro";
import SocialIcon from "@components/SocialIcon/SocialIcon.astro";
import { getLocaleFromUrl } from "@js/localeUtils";
import { getTranslatedData } from "@js/translationUtils";

import { getLocalizedRoute } from "@/js/translationUtils";

const currLocale = getLocaleFromUrl(Astro.url);
const siteData = getTranslatedData("siteData", currLocale);
const footerData = getTranslatedData("footerData", currLocale);
const today = new Date();
---

<footer class="mt-14 overflow-clip pt-16 pb-10">
	<div class="site-container">
		<div class="border-b-base-200 dark:border-b-base-800 border-b pb-10">
			<div class="flex flex-wrap">
				<div class="w-full md:w-1/2">
					<div class="max-w-xs">
						<SiteLogo />
						<p class="description mt-11 tracking-tight">
							{footerData.footerBranding.description}
						</p>
					</div>
				</div>
				<div class="w-full md:w-1/2">
					<div class="flex flex-wrap justify-between gap-x-16 gap-y-8">
						<!-- first links column -->
						<div class="w-auto py-2">
							<h3 class="text-base-800 dark:text-base-300 mb-8 font-medium tracking-tight">
								{footerData.footerLinksColumn[0].title}
							</h3>
							<ul class="flex flex-col gap-3">
								{footerData.footerLinksColumn[0].links.map((link) => (
									<li>
										<FooterLink href={getLocalizedRoute(currLocale, link.link)}>{link.text}</FooterLink>
									</li>
								))}
							</ul>
						</div>

						<!-- second links column -->
						<div class="w-auto py-2">
							<h3 class="text-base-800 dark:text-base-300 mb-8 font-medium tracking-tight">
								{footerData.footerLinksColumn[1].title}
							</h3>
							<ul class="flex flex-col gap-3">
								{footerData.footerLinksColumn[1].links.map((link) => (
									<li>
										<FooterLink href={getLocalizedRoute(currLocale, link.link)}>{link.text}</FooterLink>
									</li>
								))}
							</ul>
						</div>

						<!-- third links column -->
						<div class="w-auto py-2">
							<h3 class="text-base-800 dark:text-base-300 mb-8 font-medium tracking-tight">
								{footerData.footerLinksColumn[2].title}
							</h3>
							<ul class="flex flex-col gap-3">
								{footerData.footerLinksColumn[2].links.map((link) => (
									<li>
										<FooterLink href={getLocalizedRoute(currLocale, link.link)}>{link.text}</FooterLink>
									</li>
								))}
							</ul>
						</div>
					</div>
				</div>
			</div>
		</div>
		<div class="py-4">
			<div class="flex flex-wrap items-center justify-between">
				<div class="w-auto p-4">
					<p class="description tracking-tight">
						&copy; {today.getFullYear()}
						{" "}{siteData.name}. All rights reserved.
					</p>
				</div>
				<div class="w-auto p-4">
					<!-- Socials -->
					<div class="flex flex-col">
						<div class="flex gap-3">
							<SocialIcon
								href="https://twitter.com/BowTiedWebReapr"
								icon="tabler/brand-x"
								social="twitter (x)"
							/>
							<SocialIcon
								href="https://twitter.com/BowTiedWebReapr"
								icon="tabler/brand-facebook"
								social="facebook"
							/>
							<SocialIcon
								href="https://twitter.com/BowTiedWebReapr"
								icon="tabler/brand-google-filled"
								social="google"
							/>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</footer>
